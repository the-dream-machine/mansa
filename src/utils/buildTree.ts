import {queryCollection} from './queryCollection.js';

interface Args {
	repoName: string;
}

export const getRepoTree = async ({repoName}: Args) => {
	// const result = await queryCollection({collectionName: repoName, query: ''});
};
